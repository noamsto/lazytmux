[default.extend-words]
# Catppuccin theme variable prefix (@thm_green, @thm_bg, etc.)
thm = "thm"
# tmux layout-string checksums quoted verbatim in the bridge design docs
ba = "ba"
# tmux's sixel terminfo capability (TTYC_SXL), quoted from tty-features.c
sxl = "sxl"
